//MenuState
//
// this is a class of objects that serve to hold information
// which will then be used by the menu
function MenuState(state,y,gameState,image){
  this.state = state;
  this.y = y;
  this.gameState = gameState;
  this.image = image;
}
